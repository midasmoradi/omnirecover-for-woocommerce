<?php
/**
 * Action Scheduler registration placeholder (hooks live on RecoveryDispatcher).
 *
 * @package OmniRecover\WooCommerce
 */

namespace OmniRecover\WooCommerce\Services;

/**
 * Scheduler service.
 */
class SchedulerService {

	/**
	 * Register (reserved for future cron helpers).
	 */
	public function register() {
		// Action Scheduler callbacks are registered on RecoveryDispatcher.
	}
}
